remote_theme: "mmistakes/minimal-mistakes@4.20.1"

plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-feed
  - jekyll-include-cache

defaults:
  - scope:
      path: ""
    values:
      layout: single
      share: true
      sidebar:
        nav: "sidebar_elements"
      header:
        overlay_image: /assets/images/sinapse_logo.png
        overlay_filter: 0.5
        actions:
          - label: "Download"
            url: "https://github.com/masesgroup/sinapse/releases/"